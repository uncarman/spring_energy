#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../_javascript-obfuscator@1.2.1@javascript-obfuscator/bin/javascript-obfuscator" "$@"
  ret=$?
else 
  node  "$basedir/../_javascript-obfuscator@1.2.1@javascript-obfuscator/bin/javascript-obfuscator" "$@"
  ret=$?
fi
exit $ret
